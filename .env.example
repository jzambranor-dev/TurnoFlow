DB_HOST=localhost
DB_PORT=5432
DB_NAME=turnoflow
DB_USER=turnoflow
# CAMBIAR EN PRODUCCION — usar password fuerte (20+ chars, aleatorio)
DB_PASS=CAMBIAR_ESTE_PASSWORD

APP_ENV=local
APP_URL=http://localhost/system-horario/TurnoFlow/public
# CAMBIAR EN PRODUCCION — generar con: php -r "echo bin2hex(random_bytes(32));"
APP_SECRET=CAMBIAR_ESTE_SECRET

UPLOAD_MAX_MB=10
UPLOAD_PATH=C:/xampp/htdocs/system-horario/TurnoFlow/uploads
